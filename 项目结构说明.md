# ZPDS — 具身智能数据标准化管线

## 项目简介

ZPDS 是具身智能数据的标准化处理管线，目标是统一多个采集源（墨现、遁甲、简智新创、A2D 真机、EPIC-KITCHENS-100）的数据格式，经过质量检查、场景分割、标注后，产出可直接用于训练的标准 Prepared Segment。

**更新日期**：2026-07-25（EPIC-KITCHENS-100 数据源接入完成）

---

## 当前项目结构

```
ZPDS/
├── .gitignore                 # Git 忽略规则
├── README.md                  # 仓库说明
├── 项目结构说明.md             # 本文档
├── 功能实现说明.md             # 功能实现详细说明
├── config.yaml                # Prepared Segment 阈值配置
│
├── prepare_segment.py         # 单 Segment 生成（开发调试用）
├── batch_prepare.py           # 批量 Prepared Segment 生成（支持 --profile）
│
├── scripts/                   # 工具脚本
│   ├── batch_prepare_epic.py  # EPIC 批量 Prepared Segment 生成器
│   ├── inspect_epic_dataset.py # EPIC 数据目录扫描 → inventory.json
│   └── inspect_epic_pickle.py # EPIC Pickle protobuf 格式探测
│
├── segment/                   # Prepared Segment 生成模块
│   ├── span_determiner.py     # ① 有效时间区间判定
│   ├── video_transcoder.py    # ② 视频裁剪 + H.264 CFR 转码
│   ├── sample_map.py          # ③ 输出帧→源帧映射表生成
│   ├── imu_normalizer.py      # ④ IMU 裁剪 + 列名标准化
│   ├── calibration.py         # ⑤ 标定提取
│   ├── segment_writer.py      # ⑥ segment.json 生成（含标注流条目）
│   ├── validator.py           # ⑦ 写出后验证（含标注流 dispatch + annotation_validation.json）
│   ├── annotation_normalizer.py  # ⑧a 手-物检测 → parquet（EPIC 标注流水线）
│   ├── mask_normalizer.py     # ⑧b COCO RLE Mask → parquet（EPIC 标注流水线）
│   ├── annotation_validator.py   # ⑧c 手-物标注 10 项校验
│   ├── mask_validator.py      # ⑧d 实例分割 Mask 12 项校验
│   └── preview_generator.py   # ⑧e 标注可视化预览（bbox + interaction 叠加）
│
├── zpds_prepare/              # 异常检测 → 分段方案
│   ├── config/                # （配置统一使用根目录 config.yaml）
│   ├── readers/
│   │   ├── session_model.py   # 统一数据模型（VideoStream / ImuStream / AnnotationStream / Session）
│   │   ├── guida_reader.py    # 墨现 Guida 数据读取（MKV + JSONL + CSV）
│   │   ├── dunjia_reader.py   # 遁甲 Dunjia 数据读取（MCAP + H264 + Protobuf）
│   │   ├── umi_reader.py      # UMI 简智新创双端读取（MCAP + CompressedImage h264 + IMUMeasurement）
│   │   ├── epic_reader.py     # EPIC 视频+Pickle 标注读取（protobuf 解码 + COCO RLE 提取）
│   │   └── epic_inventory.py  # EPIC 目录结构解析 + participant/video ID 提取
│   ├── detectors/             # 5 项统一检测器（多数据源复用）
│   │   ├── frame_count.py     # 帧数一致性检查
│   │   ├── bad_frame.py       # 坏帧（解码失败）检测
│   │   ├── black_frame.py     # 黑屏区间检测 + 合并 + trim/split 决策
│   │   ├── timestamp_gap.py   # 视频时间戳缺口检测 + 分级处置
│   │   └── imu_gap.py         # IMU 时间戳缺口检测 + 分级处置
│   ├── decisions/
│   │   ├── issue_model.py     # QualityIssue 统一数据类
│   │   └── segment_planner.py # 候选 Segment 生成算法
│   ├── writers/
│   │   ├── quality_writer.py  # quality_issues.json 写入
│   │   └── candidate_writer.py # segment_candidates.json 写入
│   └── main.py                # 主入口：支持 --profile (guida/dunjia/umi/epic)
│
├── output/                    # 质量检测产出（已 gitignore）
│   ├── moxian/                # 墨现 profile 输出
│   ├── dunjia/                # 遁甲 profile 输出
│   ├── umi/                   # UMI profile 输出
│   └── epic/                  # EPIC profile 输出
│
└── prepared_segments/         # Prepared Segment 产出（已 gitignore）
    ├── moxian/                # 墨现 profile 输出
    ├── dunjia/                # 遁甲 profile 输出
    ├── umi/                   # UMI profile 输出
    └── epic/                  # EPIC profile 输出
```

## Profile 多数据源

main.py 和 batch_prepare.py 通过 `--profile` 参数支持四种数据源，**检测器 + 决策器 + 写入器 + validator 全部零改动复用**：

| Profile | 数据源 | 容器格式 | 视频 | 标注 | 标定来源 |
|---------|--------|---------|------|------|---------|
| `guida` (默认) | 墨现 | 目录 (MKV + JSONL + CSV) | color_000000.mkv, 30fps | 无 | meta.json |
| `dunjia` | 遁甲 | MCAP (Foxglove protobuf) | H264 CompressedVideo, 25fps | 无 | CameraCalibration topic |
| `umi` | 简智新创 | MCAP (Foxglove protobuf) | H264 CompressedImage, 30fps | 无 | CameraCalibration + T_b_c |
| `epic` | EPIC-KITCHENS-100 | MP4 + Pickle (protobuf) | ego_rgb.mp4, ~59.94fps | hand-object + instance segmentation | 占位标定 |

### EPIC-KITCHENS-100 特有流程

EPIC 数据源与其他三个不同——它是**公开基准数据集**，自带自动标注（手-物检测 + COCO Mask R-CNN），因此管线不经过"异常检测→分段方案"路径，而是直接从 inventory.json 批量生成 Prepared Segment：

```
EPIC-KITCHENS-100/
├── videos/
│   └── P01/
│       ├── P01_01.mp4          # 第一人称厨房视频
│       └── ...
├── hand-objects/
│   └── P01/
│       └── P01_01.pkl          # protobuf-encoded pickle: 逐帧手-物 bbox
└── masks/
    └── P01/
        └── P01_01.pkl          # COCO Mask R-CNN 实例分割

         ↓  scripts/inspect_epic_dataset.py
         ↓  生成 inventory.json（列出所有 (video, ho_pkl, mask_pkl) 三元组）

         ↓  scripts/batch_prepare_epic.py (或 batch_prepare.py --profile epic)
         ↓  逐视频完整处理

prepared_segments/epic/seg_000001/
├── segment.json                        # 核心控制文件
├── data/ego_rgb.mp4                    # H.264 CFR 转码视频
├── maps/ego_rgb_sample_map.parquet     # 输出帧↔源帧 最近邻映射
├── annotations/
│   ├── hand_objects.parquet            # 手-物检测标注（13 列）
│   └── masks.parquet                   # 实例分割标注（16 列，含 RLE）
├── calibration/calibration.json        # 占位标定
└── reports/
    ├── validation.json                 # 完整校验报告
    └── annotation_validation.json      # 标注专有校验报告
```

### EPIC 标注流水线（Steps 5-8）

```
Pickle (protobuf bytes)
    │
    ├── _decode_hand_object_pickle()    ← epic_reader.py
    │   解析 protobuf: frame_number, hand_bbox, object_bbox, score, class
    │   AnnotationStream(type="hand_object_detection", bbox_format="xyxy_normalized")
    │       │
    │       ▼ normalize_hand_objects()   ← annotation_normalizer.py
    │       │  归一化 bbox → 像素坐标, 帧映射, 实体展开
    │       ▼ write_annotation_parquet()
    │       annotations/hand_objects.parquet
    │
    └── _decode_mask_pickle()           ← epic_reader.py
       解析 protobuf: frame_number, mask_bbox, mask_rle_bytes, score, class
       AnnotationStream(type="instance_segmentation", bbox_format="xyxy_normalized")
           │
           ▼ normalize_masks()          ← mask_normalizer.py
           │  COCO RLE decode → binary mask → re-encode, bbox→像素, 帧映射
           ▼ write_mask_parquet()
           annotations/masks.parquet
```

---

## zpds_prepare/ — 异常检测 → 分段方案

将"检测到异常"变成"可执行的分段方案"。5 个检测器输出统一的 `QualityIssue`，
再由 `segment_planner` 生成候选 Segment。

### 命令

```bash
# 墨现 (默认)
python -m zpds_prepare.main "E:/datasets/egos/墨现" --output output/moxian/

# 遁甲
python -m zpds_prepare.main /path/to/session.mcap --profile dunjia --output output/dunjia/

# UMI
python -m zpds_prepare.main /path/to/01767.mcap --profile umi --output output/umi/

# EPIC（单独脚本，不经过分段方案）
python scripts/inspect_epic_dataset.py --videos-root /data/EPIC/videos --output output/epic/inventory.json
```

### 八步管线

| 步骤 | 内容 | 负责模块 |
|------|------|----------|
| ① 读取原始数据 | 根据 profile 选择 reader，统一返回 Session | `readers/guida_reader.py` / `readers/dunjia_reader.py` / `readers/umi_reader.py` |
| ②a 帧数一致性 | 对比 index 声明帧数 vs meta 声明帧数 | `detectors/frame_count.py` |
| ②b 坏帧检测 | 扫描视频文件检测解码失败帧 | `detectors/bad_frame.py` |
| ②c 黑屏区间 | 逐帧灰度均值检测 + 连续区间合并 + trim/split 决策 | `detectors/black_frame.py` |
| ②d 视频缺口 | 相邻帧间隔异常检测 + 分级处置 | `detectors/timestamp_gap.py` |
| ②e IMU 缺口 | IMU 采样间隔异常检测 + 分级处置 | `detectors/imu_gap.py` |
| ③ 汇总分析 | 按类型 / 按处置统计 Issue | `decisions/segment_planner.py` |
| ④ 写出质量报告 | quality_issues.json | `writers/quality_writer.py` |
| ⑤ 生成候选 Segment | 根据 trim/split 决策切分 Session | `decisions/segment_planner.py` |
| ⑥ 写出分段方案 | segment_candidates.json | `writers/candidate_writer.py` |

### 统一异常记录格式

```python
@dataclass
class QualityIssue:
    issue_type: str      # continuous_black_frames | timestamp_gap | imu_gap | ...
    stream_id: str       # ego_rgb | ego_imu
    start_ns: int        # 异常起始 (设备时钟)
    end_ns: int          # 异常结束 (设备时钟)
    severity: str        # warning | error | critical
    decision: str        # trim | split | keep_with_flag | quarantine
    details: dict        # 附加信息 (帧数、间隔、阈值等)
```

### 处置决策

| 位置 | 决策 | 说明 |
|------|------|------|
| 开头连续黑屏 | `trim` | 裁剪掉，不计入有效范围 |
| 结尾连续黑屏 | `trim` | 裁剪掉，不计入有效范围 |
| 中间长黑屏 | `split` | 在此处切分 |
| 中间短黑屏 | `keep_with_flag` | 保留并标记 |
| 视频小缺口 (<0.5s) | `keep_with_flag` | 保留并标记 |
| 视频大缺口 (≥0.5s) | `split` | 在此处切分 |
| IMU 小缺口 (<1.0s) | `keep_with_flag` | 保留并标记 |
| IMU 大缺口 (≥1.0s) | `split` | 在此处切分或隔离 |

### 产出结构

```
output/{moxian,dunjia,umi}/
├── quality_issues.json       # 所有异常的统一记录
└── segment_candidates.json   # 候选 Segment 列表
```

---

## segment/ — Prepared Segment 生成模块

### 管线步骤

| 步骤 | 输入 | 输出 | 负责模块 |
|------|------|------|----------|
| ① 确定有效区间 | index 索引 + QC 结果 | source_start_ns / source_end_ns | `span_determiner.py` |
| ② 视频裁剪转码 | 源视频 + Span | data/ego_rgb.mp4 (H.264 CFR) | `video_transcoder.py` |
| ③ 采样映射表 | index 索引 + Span | maps/rgb_sample_map.parquet | `sample_map.py` |
| ④ IMU 规范化 | IMU 原始数据 + Span | data/imu.parquet | `imu_normalizer.py` |
| ⑤ 标定提取 | meta.json / MCAP calibration | calibration/calibration.json | `calibration.py` |
| ⑥ 控制文件 | 全部上游结果 | segment.json | `segment_writer.py` |
| ⑦ 写出后验证 | 全部输出文件 | reports/validation.json + annotation_validation.json | `validator.py` |
| ⑧ 标注处理（EPIC） | Pickle 标注 + sample_map | annotations/{hand_objects,masks}.parquet | `annotation_normalizer.py` / `mask_normalizer.py` |

### 数据源产出结构

```
# 标准三数据源（guida / dunjia / umi）
prepared_segments/{moxian,dunjia,umi}/seg_000001/
├── segment.json                    # 核心控制文件（streams/timeline/quality）
├── data/
│   ├── ego_rgb.mp4                 # H.264 CFR 转码视频
│   └── imu.parquet                 # 规范化 IMU（相对时间 + 标准列名）
├── maps/
│   └── rgb_sample_map.parquet      # 输出帧 ↔ 源帧映射（最近邻）
├── calibration/
│   └── calibration.json            # RGB 内参 + IMU/深度 外参
└── reports/
    └── validation.json             # 自动校验

# EPIC 标注增强
prepared_segments/epic/seg_000001/
├── segment.json                    # 含 annotation role 流条目
├── data/ego_rgb.mp4
├── maps/ego_rgb_sample_map.parquet
├── annotations/                    # ← EPIC 独有
│   ├── hand_objects.parquet        # 手-物检测（13 列）
│   └── masks.parquet               # 实例分割（16 列，含 COCO RLE）
├── calibration/calibration.json
└── reports/
    ├── validation.json             # 全部校验（含标注）
    └── annotation_validation.json  # 标注专有报告
```

### write-validate 闭环

每次写出后自动验证，按 segment.json 中 stream 的 `modality` 分发：

**通用检查项（所有数据源）**：

| 检查项 | 说明 |
|--------|------|
| 文件引用完整性 | segment.json 中引用的所有文件是否真实存在 |
| 视频可解码 | 每个 video stream 能否被 OpenCV 正常打开 |
| 帧数一致 | 每个视频帧数 == 对应 sample_map 行数 |
| 映射时间单调 | 每个 output_timestamp_ns 严格递增 |
| IMU 时间单调 | 每个 timestamp_ns 非递减 |
| IMU 起点 | 每个相对时间从 0 附近开始 |

**标注检查项（EPIC hand_object_detection，10 项）**：

| 检查项 | 说明 |
|--------|------|
| annotation_file_readable | Parquet 可读取 |
| annotation_required_columns | 13 列完整性（含 entity_type、bbox、confidence、provenance） |
| annotation_timestamps_in_range | timestamp_ns 不超过 segment timeline |
| annotation_output_frame_indices_valid | output_frame_index 非负 |
| annotation_bbox_valid | bbox xyxy 坐标合法（x1≤x2, y1≤y2, 不越界） |
| annotation_confidence_range | confidence ∈ [0, 1] |
| annotation_provenance_complete | source_file/source_frame_index 不缺失 |
| annotation_mapping_error_ok | 最近邻映射误差 ≤ 50ms |
| annotation_entity_type_valid | entity_type 为 "hand" 或 "object" |

**标注检查项（EPIC instance_segmentation，12 项）**：

| 检查项 | 说明 |
|--------|------|
| mask_file_readable | Parquet 可读取 |
| mask_required_columns | 16 列完整性（含 rle_counts、rle_encoding、mask_area_px） |
| mask_timestamps_in_range | timestamp_ns 不超过 segment timeline |
| mask_output_frame_indices_valid | output_frame_index 非负 |
| mask_shape_valid | mask_height/width 匹配视频分辨率 |
| mask_not_empty | mask_area_px > 0 |
| mask_not_full | 面积比 < 98%（避免全满误检） |
| mask_bbox_coverage | bbox 面积 ≥ mask 面积的 30% |
| mask_rle_decode | RLE 能成功解码（抽样 20 条） |
| mask_rle_area_consistent | 解码面积一致性（round-trip check） |
| mask_provenance_complete | source_file/source_frame_index 追溯完整 |

---

## batch_prepare.py — 批量 Prepared Segment 生成

读取 segment_candidates.json，对每个候选区间自动生成完整 Prepared Segment。

```bash
# 墨现 (默认)
python batch_prepare.py --profile guida

# 遁甲
python batch_prepare.py --candidates output/dunjia/segment_candidates.json --profile dunjia

# EPIC
python batch_prepare.py --profile epic --dataset output/epic/record_P01_01.json
# 或使用专用批量脚本：
python scripts/batch_prepare_epic.py --inventory output/epic/inventory.json --limit 10
```

### 工作流程

```
segment_candidates.json (或 epic inventory.json)
        │
        ▼
  预加载共享资源（index 索引 + calibration）
        │
        ├── candidate_000001 → seg_000001/
        ├── candidate_000002 → seg_000002/
        └── candidate_000003 → seg_000003/
                    │
                    ▼
          prepared_segments/{moxian,dunjia,umi,epic}/
          ├── batch_summary.json       ← 批量汇总
          ├── seg_000001/              ← 完整 Prepared Segment
          ├── seg_000002/
          └── seg_000003/
```

### 共享优化

- **index 索引** — 全 Session 只读取一次
- **calibration** — 全 Session 只提取一次（标定不随 span 变化）
- 每个 Segment 独立转码视频、生成 sample_map、裁剪 IMU、写入验证

### 与 prepare_segment.py 的区别

| | prepare_segment.py | batch_prepare.py |
|---|---|---|
| 输入 | 硬编码路径 + QC 结果 | segment_candidates.json / inventory.json |
| Span 来源 | `span_determiner.py` 自动判定 | 候选方案文件中指定 |
| Segment 数量 | 始终 1 个 | 按候选方案批量 |
| 共享资源 | 无 | index + calibration 只加载一次 |
| profile 支持 | 无 | ✅ `--profile` |
| 用途 | 单次开发调试 | 批量生产 |

---

## 配置（config.yaml）

```yaml
# 视频质量检测
video:
  black_threshold: 5
  min_black_duration_s: 0.5
  black_detection:
    mean_intensity_threshold: 5.0
    min_duration_s: 0.5
    edge_tolerance_s: 1.0

# 时间戳异常检测
timestamp:
  video_gap_factor: 2.0
  imu_gap_factor: 3.0
  video:
    gap_factor: 2.0
    split_gap_s: 0.5
  imu:
    gap_factor: 3.0
    split_gap_s: 1.0

# 输出
output:
  target_fps: 30.0
  rgb_codec: "avc1"
  rgb_pix_fmt: "yuv420p"

# Segment 约束
segment:
  min_duration_s: 1.0
  max_duration_s: 120.0
```

---

## 数据源

| Profile | 路径格式 | 关键文件 |
|---------|---------|---------|
| 墨现 (guida) | 目录 | `meta.json` / `index.jsonl` / `color_000000.mkv` / `depth_000000.mkv` / `imu/imu_000000.csv` / `log/` |
| 遁甲 (dunjia) | `.mcap` 文件 | Foxglove MCAP 容器，含 CompressedVideo / CompressedImage / IMU / CameraCalibration topic（单 robot） |
| UMI 简智新创 (umi) | `.mcap` 文件 | Foxglove MCAP 容器，含 CompressedImage (h264) / IMUMeasurement / CameraCalibration（双 robot，equidistant 畸变 + T_b_c 外参） |
| EPIC-KITCHENS-100 (epic) | MP4 + Pickle | `P01_01.mp4` + `P01_01.pkl` (hand-objects) + `P01_01.pkl` (masks)；protobuf 编码的帧级标注，含 normalized bbox + COCO RLE 掩码 |

---

## 后续计划

1. ✅ 墨现清洗管线 — 已完成
2. ✅ Prepared Segment 生成 — 已完成（prepare_segment.py + segment/）
3. ✅ 异常检测 → 分段方案 — 已完成（zpds_prepare/，产出 quality_issues.json + segment_candidates.json）
4. ✅ 批量 Prepared Segment 生成 — 已完成（batch_prepare.py）
5. ✅ 墨现 + 遁甲双数据源 — 已完成（--profile guida/dunjia，5 检测器零改动复用）
6. ✅ UMI 简智新创双端夹爪 — 已完成（--profile umi，双 robot RGB+IMU+标定）
7. ✅ EPIC-KITCHENS-100 标注数据源 — 已完成（--profile epic，hand-object + mask 双标注流，22 项校验）
8. 接入 A2D 等其他数据源
9. 深度视频处理（遁甲已有深度 topic 读取）
10. 搭建标注层（CEU）和训练格式导出（LeRobot/RLDS）
