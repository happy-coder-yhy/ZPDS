# 无手部观测数据源清洗三人完整分工

> 依据：《无人体手部观测数据源清洗方案》v1.0（2026-07-30）  
> 适用范围：遁甲、简智新创 UMI、A2D，以及与墨现 Guida、EPIC-KITCHENS-100 的适用性回归  
> 目标：三人覆盖方案第 1～8 节全部内容，保持 Raw Session 不可变，以版本化质量视图、manifest、对齐表和证据表达清洗结论  
> 本文仅为实施分工，不表示对应功能已经实现

## 1. 共同原则与不可突破的边界

1. “无人体手部”不等于低质量，也不等于画面没有执行器。
2. `human_hand` 必须在模型执行前由 SourceProfile 或 session 元数据确定为：
   - `applicable`
   - `not_applicable`
   - `unavailable`
3. 遁甲、UMI、A2D 的 `human_hand=not_applicable`，不得运行人体手模型，不得产生 `HAND_ABSENT`，不得降低其他质量视图。
4. 墨现 Guida 保持 `human_hand=applicable`；EPIC 本地缺原视频时为 `unavailable`，不得伪装成“没有检测到手”。
5. 不使用单一总分决定数据去留。一个片段可以同时为：
   - `robot_observation_ready=true`
   - `robot_bc_ready=false`
   - `geometry_ready=unavailable`
6. `reject` 只表示某个质量视图不可交付，不等于删除 Raw，也不否定同一 session 在其他视图中的价值。
7. Raw Session 不修改、不覆盖、不删除；只生成版本化 manifest、对齐表、证据、预览和派生产物。
8. 禁止按帧号或行号假设相机、HDF5、ROS/MCAP 一一对应；所有映射必须记录 `direct | inferred | unavailable`、误差和不确定度。
9. 禁止跨时间 reset、长 gap、缺测或 `dt <= 0` 区域插值和计算导数。
10. 只有文件/Schema 损坏、明确时间回退、不可解码、已知安全硬限等高精度硬故障可自动 reject 对应视图。
11. 模糊、抖动、idle、lag、可见性、VIO、语义结果等阈值在金标校准前只能 `quarantine` 或 `keep_with_flag`。
12. VLM、末端分割和接触检测不全量运行，只用于低置信候选及统计抽检，并保存模型版本、提示词、配置和证据。

## 2. 三人责任总览

| 人员 | 主责任 | 独占交付 | 不负责 |
|---|---|---|---|
| A：源契约与 UMI | applicability、Profile 契约、UMI 双端同步/VIO/磁编码器、Stage 9 适用性规则 | UMI 对齐表、VIO 连续性证据、磁编码器语义状态、UMI 三项质量视图 | 不实现遁甲/A2D 专用检测器；不全量运行 VLM；不把磁编码器擅自命名为 gripper action |
| B：遁甲与 A2D | 遁甲末端/RGB-D/IMU/相机覆盖；A2D 完整性、相机—机器人对齐、state/action/夹爪/安全 | 遁甲覆盖与可见性证据；A2D completeness matrix、alignment parquet、机器人质量证据 | 不修改统一 schema；不虚构遁甲 `robot_bc_ready`；不按行号硬配 A2D |
| C：统一流水线与发布验收 | 统一质量模型、Stage 0～12 编排、物理分段、idle、语义/outcome、去重、manifest、导出、金标和回归 | 统一 Decision/Metric、quality-view 聚合、evidence index、revision manifest、验收报告 | 不代写 A/B 源级算法；不私定源级阈值；不以单一总分发布 |

## 3. 人员 A：源契约、Stage 9 与 UMI

### A1. SourceProfile 与 applicability 契约

为所有相关来源声明观测主体和质量视图：

| 来源 | `human_hand` | `end_effector` | 主质量视图 |
|---|---|---|---|
| 墨现 Guida | `applicable` | 按来源元数据 | 手部、视频、深度、IMU 相关视图 |
| 遁甲 | `not_applicable` | `applicable` | `robot_observation_ready`、`end_effector_visible` |
| UMI | `not_applicable` | `applicable` | `robot_observation_ready`、`bimanual_umi_ready`、`vio_ready` |
| A2D | `not_applicable` | `applicable` | `robot_observation_ready`、`robot_bc_ready`、`geometry_ready`、`failure_recovery` |
| EPIC 本地衍生包 | `unavailable` | 不适用 | 原视频取回前只做标注包解析隔离 |

验收：任何手模型执行前完成 applicability 判定；`not_applicable` 只生成 INFO 记录，`HAND_ABSENT` 数量必须为 0。

### A2. UMI 每流独立时间轴

分别维护 robot0/robot1 的 Camera、IMU、VIO、磁编码器：

- 保留 stream id、topic、schema、frame id、消息 timestamp、log time、publish time。
- 检查单调性、重复时间戳、回退、真实频率、gap、覆盖范围。
- 每个问题输出时间 span、reason code、建议处置及证据 URI。
- 单个辅助流失败不得否定仍可用的 RGB。

### A3. UMI 双端同步

生成 `umi_dual_alignment.parquet`：

```text
robot0_stream/sample/timestamp,
robot1_stream/sample/timestamp,
residual_ns, mapping_method, uncertainty_ns,
continuity_group, evidence_uri
```

要求：

- 只在同一连续重叠窗口内配对。
- 跨 reset/长 gap 不配对、不插值。
- 报告 overlap ratio、不可映射率、残差 P50/P95/max。
- VIO 坐标和单位确认前，不启用双端相对运动阈值。

### A4. UMI VIO

- 检查 finite、四元数范数、时间回退、长 gap、参考帧改变、位姿跳变候选。
- 形成连续组并明确组间禁止插值。
- 先确认 MCAP 是否存在显式 reset topic/field：
  - 有源信号才记录 `direct reset`；
  - 仅由 discontinuity 判断时记录 `inferred`；
  - 无法判断为 `unavailable`。
- 输出 `umi_vio_continuity.parquet/json`、坐标契约、Raw lineage 和 `vio_ready`。
- VIO 失败不得拒绝独立可用视频。

### A5. UMI 磁编码器语义

未确认物理含义前保持：

```text
raw_value
unit: unknown
semantic_status: raw_unverified
```

安全确认流程：

1. 核对设备型号、固件、schema、标定或厂商资料。
2. 用受控开合或同步视频确认是 state 还是 command。
3. 确认方向、零点、比例、单位、范围和饱和边界。
4. 两名审阅者复核并记录证据。
5. 确认后才生成版本化派生字段，同时永远保留 raw value。

未确认前只能检查 NaN/Inf、gap、freeze、range candidate；不得生成 gripper action、open/close event 或 stall 结论。

### A6. UMI 质量视图

- `robot_observation_ready`：独立可用的双路/单路视觉观测。
- `vio_ready`：VIO 时序、坐标契约和连续组可信。
- `bimanual_umi_ready`：双端同步及其声明的必要模态可信。

每个视图独立聚合；VIO 失败只阻断依赖 VIO 的视图，磁编码器语义未知为 `unavailable` 或 flag，不伪造零分。

### A7. A 的测试与交付

固定 fixture 覆盖：双端正常、单端缺流、固定偏移、时间回退、长 gap、VIO 断点、非法四元数、frame 改变、双端串流、encoder NaN/freeze/语义未确认。

关键断言：

- 两端不串流。
- 跨连续组零配对、零插值。
- 每行对齐可回溯 Raw。
- `raw_unverified` 不产生夹爪动作语义。
- `not_applicable` 不产生 `HAND_ABSENT`。
- VIO/双端视图失败不拒绝可用 RGB。

## 4. 人员 B：遁甲与 A2D

### B1. 遁甲来源完整性与相机角色

- 建立 3 路 RGB、Depth、IMU、Calibration 的完整性清单。
- 主/侧相机角色必须来自源元数据或配置，不能根据目录名或单帧猜测。
- 缺必需资产或容器不可解析才 reject 对应视图；可选流缺失为 `keep_with_flag`。
- 遁甲没有 state/action 时不得创建 `robot_bc_ready`，相关 Stage 7 项记录 `not_applicable`。

### B2. 遁甲 RGB-D

- PNG 解码、shape、dtype、unit、invalid value。
- zero/invalid/saturation ratio、冻结深度、坏帧 span。
- 对指定主 RGB 做最近邻时间配对，禁止按帧号硬配及跨 gap 配对。
- 输出 paired ratio、offset P50/P95/max、未配对 span、mapping method 和 uncertainty。
- 校验分辨率、内参和标定一致性；外参不可信只阻断 `geometry_ready` 类能力，不否定 RGB。

### B3. 遁甲 IMU

- 检查重复时间戳、回退、gap、spike、freeze、static bias candidate、saturation。
- 使用真实 `dt`；跨 gap/reset 断开。
- 设备量程有来源时才判定饱和；量程未知记 `unavailable`。

### B4. 遁甲多相机覆盖与末端可见性

生成 `dunjia_camera_coverage`：

```text
camera_id, role, role_source,
time_coverage, decode_status, bad_spans,
depth_overlap, imu_overlap,
review_sample, evidence_uri
```

- 主视角负责 RGB-D、时序和操作区判断。
- 侧视角用于遮挡互补及末端可见性复核。
- 有可信几何时投影末端，统计 in-frame ratio 和重投影残差。
- 无可靠几何时采用稀疏人工复核；VLM 只用于低置信候选并标 `model_estimated`。
- 输出 `end_effector_visible` 的可见率、不可见 span、方法、证据、版本和配置 hash。

### B5. 遁甲质量视图

- `robot_observation_ready`
- `end_effector_visible`

末端不可见与任务成功分别报告；Depth/IMU/几何子视图失败不得连带拒绝可用 RGB。

### B6. A2D 完整性矩阵

读取并核验：

- `meta_info.json`
- head/left/right × color/depth
- RGB-D Calibration
- `aligned_joints.h5` / `raw_joints.h5`
- ROS2 MCAP
- 设备日志
- review/action span

生成 A2D completeness matrix，核对 duration、clip 边界、相机帧数、HDF5/ROS 消息数及相互矛盾。

### B7. A2D 相机—机器人对齐

生成 `camera_robot_alignment.parquet`：

```text
camera_stream_id, source_frame_index,
camera_timestamp_ns, robot_row, robot_timestamp_ns,
mapping_method, error_ns, uncertainty_ns,
continuity_group, evidence_uri
```

- 不能把第 N 个 HDF5 行视为第 N 个相机帧。
- 禁止跨 reset/长 gap 插值。
- 映射不可信时保留 RGB，但阻断 `robot_bc_ready`。

### B8. A2D state/action/夹爪/安全

- 有限性、维度、joint name、单位、控制模式。
- 时间 gap、覆盖率、freeze、NaN/Inf、关节越限、安全错误码。
- 速度、加速度、jerk 使用真实 `dt`；先按中位数/MAD 候选，再以设备额定范围和人工证据复核。
- state/action 分别保留时间轴；可信重叠窗口估计 lag，输出 P50/P95 和符号。
- 对夹爪命令核对位置/速度变化，区分无动作与有命令无响应。
- action 不可信时禁用 BC 视图，但视频仍可保留。

### B9. A2D 质量视图

- `robot_observation_ready`
- `robot_bc_ready`
- `geometry_ready`
- `failure_recovery`

技术损坏与操作失败分开；failure、recovery、incomplete 默认保留并写 outcome，只有下游 release 明确只收成功轨迹时才过滤。

### B10. B 的测试与交付

遁甲 fixture：末端可见但无人手、相机缺失、坏 PNG、时间回退/长 gap、Depth 全零/冻结、RGB-D 偏移、IMU gap/freeze/饱和、量程未知、主视角遮挡但侧视角可见、无标定、全段末端不可见。

A2D fixture：缺相机文件、末帧不完整、帧号稀疏、HDF5/相机长度不同、时间回退、跨长 gap 映射、关节 NaN/越限、夹爪有命令无响应、安全错误码、对齐不可恢复。

关键断言：遁甲不创建 `robot_bc_ready`；A2D BC 失败不拒绝 RGB；所有问题带时间 span、映射方法和证据 URI；Raw hash 不变。

## 5. 人员 C：统一质量模型、分段、语义、去重与交付

### C1. 统一质量契约

统一现有 QualityMetric、Decision、QualityReport 与 QualityIssue，至少保存：

```text
metric_name, value, unit, applicability,
severity, disposition, reason_code,
start_ns, end_ns, evidence_uri,
producer, version, config_hash
```

质量视图按依赖独立聚合，不再仅依赖 `overall_pass`。处置统一为：

- `keep`
- `keep_with_flag`
- `quarantine`
- `trim`
- `split`
- `reject(view)`

### C2. Stage 0～12 编排与主入口

| Stage/层 | 责任 |
|---|---|
| 0～1 | inventory、SHA-256、容器、schema、必需流完整性 |
| 2 | 每流时间轴、回退、gap、映射方法和不确定度 |
| 3～4 | 黑屏、过曝、模糊、冻结、坏帧、视频时序 |
| 5 | Depth 有效性、冻结、RGB-D 配对；调用 B 的源级结果 |
| 6 | IMU gap/spike/freeze/bias/saturation；调用 A/B 的源级结果 |
| 7 | 机器人 state/action/夹爪/安全；遁甲记 not applicable |
| 8 | 标定、分辨率、投影、重投影、几何视图 |
| 9 | applicability 前置；机器人源不得产生 `HAND_ABSENT` |
| 10 | 任务语义、success/failure/recovery/incomplete/unknown |
| 11 | 精确去重、近重复候选、分布偏差和 split 泄漏检查 |
| 12 | 交付、schema、hash、证据和导出验证 |

C 负责将 A/B 的 typed decisions 和 evidence index 接入统一流水线，但不重写 A/B 算法。

### C3. Physical span 与 idle

- 根据相机、状态和动作的可信公共范围生成 physical span。
- 明确技术坏画面、时间 gap、不可用映射、硬件故障的连续区间。
- 首尾 idle 使用三信号联合：机器人运动能量 + 夹爪事件 + 视觉变化。
- 只自动提出首尾裁剪候选；中段低运动默认保留。
- 技术中断 `split`；不确定等待 `keep_with_flag`。
- 默认只输出候选和预览，人工确认后才固化阈值。

### C4. 任务语义与结果

- 优先导入已有 review/action span，并校验 frame-id 命名空间。
- 对低置信、异常或抽样片段生成多相机画面、状态曲线和夹爪事件叠加预览。
- VLM/人工复核输出 `success | failure | recovery | incomplete | unknown`，附时间和证据。
- VLM 结果不能覆盖源真值，必须标明模型、提示词、配置和版本。
- 操作失败/恢复与技术损坏分开存储。

### C5. 去重、偏差与数据切分

- SHA-256 自动处理精确重复。
- 视频 pHash/embedding 只生成近重复候选，进入复核。
- 状态轨迹按时间归一化后使用相关性或 DTW 生成候选。
- 按 session/group 切分，避免训练/验证泄漏。
- 比较清洗前后 source、device、task、scene、action amplitude、outcome 分布。
- 如果清洗后只剩容易、慢速或单任务数据，阻断发布。

### C6. Manifest、证据和导出

segment/revision manifest 必须保存：

- source span 和 revision；
- quality views 及其 disposition；
- applicability；
- outcome；
- reason code；
- evidence URI；
- producer/version；
- config hash；
- Raw lineage。

证据 URI 必须可解析，配置变化必须改变 config hash，重复执行结果应确定。C 同时负责对 LeRobot/RLDS 等导出执行 schema、时间轴和可追溯性验证；训练任务从 release 选择视图，不在清洗阶段物理删除其他数据。

### C7. 金标与阈值校准

- 遁甲、UMI、A2D 各按任务、相机、时长、结果分层至少抽取 30 episode；不足则全量。
- 至少两名审阅者独立标注：技术损坏 span、末端/工作区可见性、首尾无效 idle、操作结果、恢复、标定问题。
- A2D 追加标注是否足以做 state-action 监督。
- UMI 追加标注双端同步和 VIO 是否可用。
- 分歧进行 adjudication，并报告一致率。

验收报告：

- 硬故障规则 precision 接近 100%。
- 对齐 precision/recall，按 `direct` 和 `inferred` 分层。
- idle 边界误差、误裁有效动作比例、中段等待保留率。
- 每个 reason code 的 precision、recall 和误报原因。
- 不使用单一总分。

### C8. 全链路回归

固定 fixture 覆盖：

- 三个机器人源无人手但末端可见；
- 墨现有人手；
- EPIC 缺原视频；
- 缺相机文件、坏尾帧、稀疏帧号；
- HDF5/相机长度不同；
- 时间回退、长 gap；
- 关节 NaN/越限；
- 夹爪有命令无响应；
- UMI VIO reset/断点；
- 首尾 idle、中段正常等待；
- 失败后恢复；
- 几何不可用但 RGB 可用。

全局硬断言：

1. `not_applicable` 不产生 `HAND_ABSENT`。
2. `robot_bc_ready` 失败不拒绝仍可用的 RGB 质量视图。
3. 遁甲不虚构 `robot_bc_ready`。
4. VIO 失败不拒绝独立可用视频。
5. 跨 reset/gap 不插值。
6. 每个非 NA 结论都有可读证据 URI。
7. Manifest schema 可 round-trip。
8. Raw 文件和 hash 不变。

## 6. 执行顺序与并行关系

```text
阶段 1：C 冻结统一 schema、applicability、quality-view 依赖和 evidence 接口
                              ↓
阶段 2：A 实现 UMI                     B 实现遁甲与 A2D
        双端同步/VIO/编码器             RGB-D/IMU/覆盖/对齐/机器人质量
                              ↓
阶段 3：C 接入 Stage 0～12、主入口、manifest、分段、语义和去重
                              ↓
阶段 4：A/B 完成源级测试，C 完成跨源和全链路回归
                              ↓
阶段 5：三源金标、阈值校准、分布验收、导出验证
```

## 7. 文件冲突与协作规则

1. A 独占 UMI reader/detector/evidence writer 和 UMI 测试。
2. B 独占遁甲、A2D reader/detector/evidence writer 和源级测试。
3. C 独占 Profile 基类、统一质量 schema、Stage 注册/主入口、manifest、revision、集成测试和发布报告。
4. A/B 需要公共字段时提交接口需求，由 C 统一修改公共文件。
5. C 不直接修改 A/B 的源级检测算法或阈值。
6. A/B 的输出必须满足统一字段契约，否则不得接入主流程。

## 8. 原方案覆盖确认矩阵

| 原方案部分 | 覆盖内容 | 负责人 | 状态 |
|---|---|---|---|
| §1 结论 | 五项清洗目标、手部可选观测、多质量视图 | A/C | 已纳入 |
| §2.1 数据源审计 | Guida、遁甲、UMI、A2D、EPIC applicability | A | 已纳入 |
| §2.2 三种适用性 | applicable/not_applicable/unavailable、Stage 9 前置 | A/C | 已纳入 |
| §2.3 非单一总分 | 独立 quality views、完整 metric 字段、Raw 不可变 | C | 已纳入 |
| §3 设计原则 | 多模态、真实时间轴、失败恢复、标定验证、来源语义保留 | 全员 | 已纳入 |
| §4 Stage 0～1 | inventory/hash/schema/必需流 | B/C | 已纳入 |
| §4 Stage 2 | 时间、gap、相机—机器人映射 | A/B/C | 已纳入 |
| §4 Stage 3～5 | 画面、视频时序、RGB-D | B/C | 已纳入 |
| §4 Stage 7 状态 | NaN、越限、freeze、速度/加速度/jerk | B/C | 已纳入 |
| §4 Stage 7 命令/夹爪 | action、lag、覆盖、夹爪响应、安全 | B/C | 已纳入 |
| §4 Stage 8 | 标定、投影、末端/工作区可见性 | B/C | 已纳入 |
| §4 Physical/idle | 三信号、首尾 trim、中断 split、中段等待保留 | C | 已纳入 |
| §4 Stage 10 | 语义、结果、失败、恢复、证据预览 | C | 已纳入 |
| §4 Stage 11 | 精确/近重复、DTW、泄漏与分布偏差 | C | 已纳入 |
| §4.1 阈值 | 硬故障自动化、金标前 quarantine/flag、真实 dt | 全员 | 已纳入 |
| §5 流水线 | 完整性→时间→技术/机器人→span→idle→语义→manifest | C，A/B 提供源结果 | 已纳入 |
| §5.1 遁甲 | RGB-D、IMU、相机覆盖、末端可见性 | B | 已纳入 |
| §5.1 UMI | 双端同步、VIO、磁编码器 | A | 已纳入 |
| §5.1/5.2 A2D | 完整性、对齐、state/action、BC 视图 | B | 已纳入 |
| §6 统一处置 | reject(view)、trim、split、quarantine、flag、outcome | C | 已纳入 |
| §7.1 最小实现 | Profile、适用性、源检测器、证据、idle、manifest | 全员 | 已纳入 |
| §7.2 金标验收 | 三源≥30、双审阅、仲裁、precision/recall/分布 | C 组织，A/B 支持 | 已纳入 |
| §7.3 回归 | 全部固定 fixture 和两项核心断言 | C 统筹 | 已纳入 |
| §8 当前边界 | 不全量重模型、A2D 不宣称伪同步、可见性≠成功、骨架不算实现 | 全员 | 已纳入 |

## 9. 覆盖结论

本分工已经覆盖原清洗方案第 1～8 节的全部实施内容，包括：适用性、三类机器人源、分层技术清洗、机器人状态动作、几何与可见性、physical span、idle、语义/outcome、失败恢复、去重偏差、统一处置、质量视图、证据/manifest、金标校准、回归测试及当前边界。

任何后续新增工作若无法落入上述覆盖矩阵，应先更新本分工和统一契约，再进入实现。
