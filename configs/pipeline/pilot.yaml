# ZPDS Pilot 阶段流水线配置
pipeline:
  version: "0.1.0"
  stages: [0, 1, 2, 3, 6]  # pilot 阶段只跑基础检查
  stop_on_fatal: true
  parallel_stages: false

segmentation:
  min_segment_duration_s: 1.0
  idle_trim_seconds: 0.5

prepared:
  preserve_source_rate: true
  depth_format: "zarr"
  rgb_codec: "h264"

# Alignment 是派生视图，不改写 Prepared 中各流的原始频率。
alignment:
  enabled: true
  target_fps: 30
  method: "nearest"

runner:
  max_retries: 1
  retry_backoff_seconds: 0

export:
  lerobot_version: "v3"
