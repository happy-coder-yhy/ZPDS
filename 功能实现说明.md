# ZPDS 功能实现说明

> 生成日期：2026-07-23
> 更新日期：2026-07-25（加入 EPIC-KITCHENS-100 标注数据源）

---

## 一、当前整体架构

```
              墨现 Guida           遁甲 Dunjia           UMI 简智新创         EPIC-KITCHENS-100
         (MKV + JSONL + CSV)      (.mcap 文件)         (.mcap 文件, 双robot)  (MP4 + Pickle protobuf)
                  │                    │                     │                    │
                  ▼                    ▼                     ▼                    ▼
         guida_reader.py       dunjia_reader.py       umi_reader.py       epic_reader.py
                  │                    │                     │                    │
                  └────────────────────┼─────────────────────┴────────────────────┘
                                       │
                                       ▼
                            zpds_prepare/main.py             batch_prepare.py
                         --profile guida/dunjia/umi     --profile guida/dunjia/umi/epic
                         5 项检测 → 分段方案              批量生成 Prepared Segment
                         ① 帧数一致性  ② 坏帧                 │
                         ③ 黑屏区间    ④ 视频缺口              │
                         ⑤ IMU 缺口                           ▼
                                       │        prepared_segments/{moxian,dunjia,umi,epic}/
                     output/{moxian,dunjia,umi}/         ├── batch_summary.json
                     ├── quality_issues.json             ├── seg_000001/
                     └── segment_candidates.json         ├── seg_000002/
                                                        └── ...

EPIC 单独路径 (不经过 QC → 分段方案):
  inventory.json → batch_prepare_epic.py → prepared_segments/epic/seg_000001/
```

**核心设计**：5 个检测器 + 2 个决策器 + 2 个写入器 + validator 全部零改动复用，仅通过 reader 适配不同数据源。reader 统一返回 Session 对象（含 `video_streams` / `imu_streams` / `annotation_streams` 字典），调用方遍历流处理，不再硬编码文件名。

---

## 二、命令

### 命令 1：异常检测 → 分段方案

```bash
# 墨现 (默认 profile)
python -m zpds_prepare.main "E:/datasets/egos/墨现" --output output/moxian/

# 遁甲
python -m zpds_prepare.main /path/to/session.mcap --profile dunjia --output output/dunjia/

# UMI
python -m zpds_prepare.main /path/to/01767.mcap --profile umi --output output/umi/
```

**6 步管线**：

| 步骤 | 检测项 | 模块 | issue_type |
|------|--------|------|-----------|
| 1 | 读取原始数据（按 profile 分发 reader，统一返回 Session） | `readers/guida_reader.py` / `readers/dunjia_reader.py` / `readers/umi_reader.py` | — |
| 2a | 帧数一致性 | `detectors/frame_count.py` | `frame_count_mismatch` |
| 2b | 坏帧检测 | `detectors/bad_frame.py` | `bad_frame` |
| 2c | 黑屏区间 | `detectors/black_frame.py` | `continuous_black_frames` |
| 2d | 视频时间戳缺口 | `detectors/timestamp_gap.py` | `timestamp_gap` |
| 2e | IMU 时间戳缺口 | `detectors/imu_gap.py` | `imu_gap` |
| 3 | 汇总分析 | `decisions/segment_planner.py` | — |
| 4 | 写出 quality_issues.json | `writers/quality_writer.py` | — |
| 5 | 生成候选 Segment | `decisions/segment_planner.py` | — |
| 6 | 写出 segment_candidates.json | `writers/candidate_writer.py` | — |

### 命令 2：批量生成 Prepared Segment

```bash
# 墨现 (默认)
python batch_prepare.py --profile guida

# 遁甲
python batch_prepare.py --candidates output/dunjia/segment_candidates.json --profile dunjia

# UMI
python batch_prepare.py --profile umi --dataset /path/to/01767.mcap -c output/umi/segment_candidates.json -o prepared_segments/umi/
```

### 命令 3：EPIC-KITCHENS-100 专用

```bash
# ① 生成 inventory（扫描数据集目录）
python scripts/inspect_epic_dataset.py \
  --videos-root /data/EPIC-KITCHENS-100/videos \
  --annotations-root /data/EPIC-KITCHENS-100 \
  --output output/epic/inventory.json

# ② 探测 Pickle 格式
python scripts/inspect_epic_pickle.py \
  --pickle /data/EPIC-KITCHENS-100/hand-objects/P01/P01_01.pkl \
  --max-frames 5

# ③ 单视频处理
python batch_prepare.py --profile epic \
  --dataset output/epic/record_P01_01.json \
  --output prepared_segments/epic/

# ④ 批量处理（支持 --limit --resume --failed-only --participant）
python scripts/batch_prepare_epic.py \
  --inventory output/epic/inventory.json \
  --limit 10 \
  --output-root prepared_segments/epic/
```

---

## 三、模块细分

### 3.1 zpds_prepare/ — 异常检测 → 分段方案

| 文件 | 作用 | 关键函数 |
|------|------|----------|
| `main.py` | 6 步主程序 + profile 分发，遍历 session 流 | `main()`, `_get_reader()` |
| `readers/session_model.py` | 统一数据模型 | `Session`, `VideoStream`, `ImuStream`, `AnnotationStream` dataclass |
| `readers/guida_reader.py` | 墨现结构化读取 | `read_session()`, `read_meta()`, `read_index_frames()`, `read_imu()` |
| `readers/dunjia_reader.py` | 遁甲 MCAP 读取 | `read_session()`, `read_meta()`, `read_index_frames()`, `read_imu()` |
| `readers/umi_reader.py` | UMI 双端 MCAP 读取 | `read_session()`, `read_calibration()`, `reconstruct_video()` |
| `readers/epic_reader.py` | EPIC 视频+Pickle 标注读取 | `read_session()`, `probe_video()`, `read_frame_pts_ns()` |
| `readers/epic_inventory.py` | EPIC 目录结构解析 | `build_inventory()`, `parse_epic_id()` |
| `decisions/issue_model.py` | 统一异常模型 | `QualityIssue` dataclass |
| `detectors/frame_count.py` | 帧数一致性 | `detect_frame_count_mismatch()` |
| `detectors/bad_frame.py` | 坏帧检测 + 区间合并 | `detect_bad_frames()` |
| `detectors/black_frame.py` | 黑屏合并+决策 | `find_continuous_true_spans()`, `decide_black_span()` |
| `detectors/timestamp_gap.py` | 视频缺口分级 | `detect_timestamp_gaps()`, `decide_gap_action()` |
| `detectors/imu_gap.py` | IMU 缺口分级 | `detect_imu_gaps()` |
| `decisions/segment_planner.py` | 候选 Segment 生成 | `plan_segments()`, `get_issue_summary()` |
| `writers/quality_writer.py` | quality_issues.json | `write_quality_issues()` |
| `writers/candidate_writer.py` | segment_candidates.json | `write_segment_candidates()` |

### 3.2 segment/ — Prepared Segment 生成模块

| 文件 | 作用 | 关键函数 |
|------|------|----------|
| `span_determiner.py` | 有效区间判定 | `determine_span()`, `load_index()` |
| `video_transcoder.py` | 视频→MP4 CFR 转码 | `transcode_rgb()` |
| `sample_map.py` | 最近邻映射表 | `generate_sample_map()`, `write_sample_map()` |
| `imu_normalizer.py` | IMU 裁剪+标准化 | `normalize_imu()`, `write_imu()` |
| `calibration.py` | 标定提取（墨现/遁甲双路径） | `extract_calibration()`, `extract_calibration_from_mcap()` |
| `segment_writer.py` | segment.json 生成（含标注流条目） | `build_segment_json()`, `write_segment_json()` |
| `validator.py` | 写出后验证 + 标注 dispatch | `validate_segment()`, `write_validation_report()`, `write_annotation_validation_report()` |
| `annotation_normalizer.py` | 手-物检测 → parquet | `normalize_hand_objects()`, `write_annotation_parquet()` |
| `mask_normalizer.py` | COCO RLE Mask → parquet | `normalize_masks()`, `write_mask_parquet()`, `decode_rle()`, `encode_binary_mask()` |
| `annotation_validator.py` | 手-物标注 10 项校验 | `validate_hand_object_stream()` |
| `mask_validator.py` | 实例分割 12 项校验 | `validate_mask_stream()` |
| `preview_generator.py` | 标注可视化预览 | `generate_previews()`, `_draw_bbox()`, `_draw_info_panel()` |

### 3.3 顶层脚本

| 文件 | 作用 |
|------|------|
| `prepare_segment.py` | 单 Segment 生成（开发调试用，不支持 profile） |
| `batch_prepare.py` | 批量 Prepared Segment 生成（生产管线，支持 `--profile guida/dunjia/umi/epic`） |
| `scripts/inspect_epic_dataset.py` | EPIC 数据集目录扫描，生成 inventory.json |
| `scripts/inspect_epic_pickle.py` | EPIC protobuf-encoded Pickle 格式探测 |
| `scripts/batch_prepare_epic.py` | EPIC 专用批量生成器（`--limit`, `--participant`, `--resume`, `--failed-only`） |

### 3.4 Profile 分发机制

```python
def _get_reader(profile: str):
    """所有 reader 模块导出统一的 read_session() 入口"""
    if profile == "guida":
        from zpds_prepare.readers import guida_reader as rd
    elif profile == "dunjia":
        from zpds_prepare.readers import dunjia_reader as rd
    elif profile == "umi":
        from zpds_prepare.readers import umi_reader as rd
    elif profile == "epic":
        from zpds_prepare.readers import epic_reader as rd
    return rd
```

所有 reader 统一返回 `Session` 对象（含 `video_streams` / `imu_streams` / `annotation_streams` 字典），调用方遍历流处理，不再硬编码文件名。

| Profile | 视频 | IMU | 标注 | 流 ID | 畸变模型 |
|---------|------|-----|------|-------|---------|
| `guida` | MKV MJPG, 30fps | CSV, 50Hz | 无 | `ego_rgb`, `ego_imu` | — (从 meta.json) |
| `dunjia` | CompressedVideo H264, 25fps | foxglove.Imu, 196Hz | 无 | `camera0/1/2`, `robot0_imu` | `plumb_bob` |
| `umi` | CompressedImage H264, 30fps | IMUMeasurement, ~195Hz | 无 | `robot0/1_camera0`, `robot0/1_imu` | `equidistant` + `T_b_c` |
| `epic` | MP4, ~59.94fps | 无 | hand_object_detection + instance_segmentation | `ego_rgb`, `hand_objects`, `masks` | 占位标定 |

---

## 四、统一数据模型

### 4.1 QualityIssue（异常）

所有 5 个检测器的输出统一为此数据类：

```python
@dataclass
class QualityIssue:
    issue_type: str              # frame_count_mismatch | bad_frame | continuous_black_frames | timestamp_gap | imu_gap
    stream_id: str               # ego_rgb | ego_imu
    start_ns: int                # 异常起始 (设备时钟)
    end_ns: int                  # 异常结束 (设备时钟)
    severity: str                # warning | error | critical
    decision: str                # trim | split | keep_with_flag
    details: dict                # 检测器附加信息
```

### 5 种 issue_type 速查

| issue_type | 检测器 | 典型 decision |
|-----------|--------|--------------|
| `frame_count_mismatch` | `frame_count.py` | `keep_with_flag`（差异≤5 → warning，>5 → error） |
| `bad_frame` | `bad_frame.py` | `keep_with_flag`（标记，不切分） |
| `continuous_black_frames` | `black_frame.py` | 首尾 → `trim`，中间长 → `split` |
| `timestamp_gap` | `timestamp_gap.py` | ≥0.5s → `split`，<0.5s → `keep_with_flag` |
| `imu_gap` | `imu_gap.py` | ≥1.0s → `split`，<1.0s → `keep_with_flag` |

### 4.2 Session / AnnotationStream（含标注的数据模型）

```python
@dataclass
class AnnotationStream:
    """单个标注流的数据。承载 EPIC-KITCHENS-100 等数据源的帧级标注。"""
    stream_id: str               # "hand_objects", "masks"
    annotation_type: str          # "hand_object_detection" | "instance_segmentation"
    source_path: Path             # 标注 Pickle 文件路径
    source_video_stream_id: str   # 关联的 VideoStream.stream_id
    frame_index_base: int         # 帧号基数 (0 = 0-based, 1 = 1-based)
    bbox_format: str | None       # "xyxy_normalized" | "xyxy_pixels"
    records: list[dict]           # 帧级标注记录列表
    metadata: dict                # 附加元数据

@dataclass
class Session:
    """一次采集 Session 的全部流数据。"""
    session_id: str
    source_path: str
    meta: dict                    # device, fps, width, height, frame_count, ...
    video_streams: dict[str, VideoStream]
    imu_streams: dict[str, ImuStream]
    annotation_streams: dict[str, AnnotationStream]   # ← EPIC 数据源使用
```

---

## 五、候选 Segment 生成算法

```
输入: issues (list[QualityIssue]), session_start_ns, session_end_ns

1. 分离 issue:
   - decision = "trim" 且靠近起始 → head_trims
   - decision = "trim" 且靠近末尾 → tail_trims
   - decision = "split" → split_boundaries

2. 计算有效范围:
   valid_start = max(session_start, 最晚 head_trim.end)
   valid_end   = min(session_end,   最早 tail_trim.start)

3. 切分:
   按 split_boundaries 中的缺口切割
   [valid_start, gap1.start], [gap1.end, gap2.start], ...

4. 过滤:
   - 丢弃 duration < min_duration_s (默认 1s)
   - 标记 duration > max_duration_s (默认 120s)
```

---

## 六、EPIC-KITCHENS-100 标注流水线详解

### 6.1 Pickle → Protobuf 解析

EPIC 自动标注存储为 **protobuf-encoded pickle**（`list[bytes]`），每条 bytes 是一条帧级检测结果：

```
protobuf Entry (hand-object):
  field_1: string   "hand_object"
  field_2: varint   frame_number (1-based)
  field_3: message  object { bbox(1), score(2), noun_class(3), contact_offset(4) }
  field_4: message  hand { bbox(1), score(2) }

protobuf Entry (mask):
  field_1: string   "mask"
  field_2: varint   frame_number (1-based)
  field_3: message  mask { bbox(1), rle_counts(2, bytes), score(3), category_id(4) }
```

**关键修复**：protobuf wire type 2 二进制字段（RLE counts）如果按 UTF-8 解码会损坏。`_parse_proto_entry()` 在递归解析返回空 dict 时保留原始 `bytes`，保证 COCO RLE 数据完整。

### 6.2 标注规范化流程

```
AnnotationStream (records: list[dict])
    │
    ├── hand_object_detection → normalize_hand_objects()
    │   1. 帧映射: 标注帧 → 最近邻输出帧 (binary search)
    │   2. bbox 转换: xyxy_normalized [0,1] → xyxy_pixels (× video_width/height)
    │   3. 实体展开: 每条 record → 2 行 (hand + optional object)
    │   4. 写入: annotations/hand_objects.parquet (13 列)
    │
    └── instance_segmentation → normalize_masks()
        1. 帧映射: 标注帧 → 最近邻输出帧
        2. bbox 转换: normalized → pixels
        3. RLE 处理: bytes → pycocotools.decode() → binary mask → mask.encode()
        4. 面积计算: bbox 面积 + mask 面积
        5. 写入: annotations/masks.parquet (16 列, 含 rle_counts + rle_encoding)
```

### 6.3 Hand-Object Parquet Schema（13 列）

| 列名 | 类型 | 说明 |
|------|------|------|
| `timestamp_ns` | int64 | 输出时间戳（相对） |
| `output_frame_index` | int64 | 输出帧索引 |
| `source_timestamp_ns` | int64 | 源时间戳（设备时钟） |
| `source_frame_index` | int64 | 源帧索引 |
| `entity_type` | str | "hand" / "object" |
| `bbox_x1`, `bbox_y1`, `bbox_x2`, `bbox_y2` | float64 | 像素坐标 bbox |
| `confidence` | float64 | 检测置信度 [0,1] |
| `object_class` | int64 | 物体类别 ID（仅 entity_type="object"） |
| `contact_x`, `contact_y` | float64 | 接触点偏移（仅 entity_type="object"） |
| `source_file` | str | 来源 Pickle 路径 |

### 6.4 Mask Parquet Schema（16 列）

| 列名 | 类型 | 说明 |
|------|------|------|
| `timestamp_ns` | int64 | 输出时间戳（相对） |
| `output_frame_index` | int64 | 输出帧索引 |
| `source_timestamp_ns` | int64 | 源时间戳 |
| `source_frame_index` | int64 | 源帧索引 |
| `instance_id` | int64 | 实例 ID |
| `category_id` | int64 | COCO 类别 ID |
| `category_name` | str | COCO 类别名 |
| `score` | float64 | 检测置信度 |
| `bbox_x1`, `bbox_y1`, `bbox_x2`, `bbox_y2` | float64 | 像素坐标 bbox |
| `mask_height`, `mask_width` | int64 | 掩码尺寸 |
| `rle_counts` | bytes | COCO RLE counts（二进制） |
| `rle_encoding` | str | "coco_rle" |
| `mask_area_px` | int64 | 掩码覆盖像素数 |
| `source_file` | str | 来源 Pickle 路径 |

### 6.5 Validator 标注 Dispatch

`validate_segment()` 遍历 segment.json 的 annotation streams，按 `modality` 分发：

```python
if modality == "hand_object_detection":
    result = validate_hand_object_stream(seg_dir, stream, ...)  # 10 checks
elif modality == "instance_segmentation":
    result = validate_mask_stream(seg_dir, stream, ...)          # 12 checks
```

`write_annotation_validation_report()` 从完整 validation 中提取标注相关条目，单独写出 `reports/annotation_validation.json`。

### 6.6 Preview Generator

`generate_previews()` 在视频帧上叠加标注可视化：
- **手 bbox**：绿色矩形 + "Hand" 标签
- **物体 bbox**：蓝色矩形 + "Object" 标签
- **交互连线**：手中心 → 物体中心的黄色虚线
- **信息面板**：帧号、检测置信度、物体类别

---

## 七、Prepared Segment 产出结构

### 7.1 标准结构（guida / dunjia / umi）

```
prepared_segments/{moxian,dunjia,umi}/seg_000001/
├── segment.json                    # 核心控制文件
│   ├── segment_id, source_session
│   ├── source_assets (源文件 SHA-256)
│   ├── timeline (相对时钟: 0 → duration_ns)
│   ├── source_span (设备时钟范围)
│   ├── streams[ego_rgb + ego_imu]
│   ├── calibration_uri
│   └── quality (status + issues，从 candidates 传入)
├── data/
│   ├── ego_rgb.mp4                 # H.264 CFR 转码视频
│   └── imu.parquet                 # 相对时间 + 标准列名
├── maps/
│   └── rgb_sample_map.parquet      # 输出帧↔源帧 最近邻映射
├── calibration/
│   └── calibration.json            # 针孔模型内参 + 外参
└── reports/
    └── validation.json             # 自动校验
```

### 7.2 EPIC 增强结构

```
prepared_segments/epic/seg_000001/
├── segment.json                    # 含 annotation role 流条目
│   └── streams: [
│         {stream_id: "ego_rgb", role: "video", ...},
│         {stream_id: "hand_objects", role: "annotation",
│          modality: "hand_object_detection",
│          origin: {kind: "imported_model_annotation", sample_map_uri: "maps/..."}},
│         {stream_id: "masks", role: "annotation",
│          modality: "instance_segmentation", ...},
│       ]
├── data/ego_rgb.mp4
├── maps/ego_rgb_sample_map.parquet
├── annotations/                    # ← EPIC 独有
│   ├── hand_objects.parquet
│   └── masks.parquet
├── calibration/calibration.json
└── reports/
    ├── validation.json             # 全部校验（含 22 项标注检查）
    └── annotation_validation.json  # 标注专有报告
```

---

## 八、当前覆盖范围

### 已实现

- [x] 5 项统一检测（帧数/坏帧/黑屏/视频缺口/IMU缺口）— 多数据源零改动复用
- [x] QualityIssue 统一输出 + quality_issues.json
- [x] 候选 Segment 自动规划 + segment_candidates.json
- [x] 批量 Prepared Segment 生成（batch_prepare.py，支持 `--profile`）
- [x] 单 Segment 生成（prepare_segment.py，开发调试用）
- [x] 共享资源优化（index + calibration 只加载一次）
- [x] write-validate 闭环验证（按 stream_id + modality 动态扩展）
- [x] SHA-256 源资产锁定
- [x] 最近邻 CFR 重采样 + sample_map 映射表
- [x] Reader 统一返回 Session（video_streams / imu_streams / annotation_streams 字典）
- [x] 检测器遍历所有流（for stream in session.video_streams.values()）
- [x] 动态文件名（由 stream_id 决定，不再硬编码）
- [x] 墨现 + 遁甲 + UMI 三数据源 profile 分发（reader 替换，检测器全复用）
- [x] 遁甲多摄像头支持（camera0/1/2）+ 深度视频读取
- [x] UMI 简智新创双端夹爪（双 robot RGB + IMU + equidistant 标定 + T_b_c）
- [x] EPIC-KITCHENS-100 数据源接入（hand-object detection + instance segmentation）
- [x] Protobuf-encoded Pickle 解析（wire type 0/2/5，递归解码 + 二进制 RLE 保护）
- [x] COCO RLE 掩码编解码（pycocotools decode/encode round-trip）
- [x] 标注规范化（帧映射、bbox normalized→pixels、实体展开）
- [x] 标注校验（hand-object 10 项 + mask 12 项，含 RLE decode + area consistency）
- [x] 标注预览可视化（bbox + interaction link + info panel）
- [x] 分离的 annotation_validation.json 报告
- [x] EPIC 批量生成器（--limit / --resume / --failed-only / --participant）
- [x] EPIC 数据集目录扫描（inventory.json 生成）

### 尚未实现

- [ ] 深度视频转码输出（当前只输出 RGB）
- [ ] UMI 磁编码器 + VIO 末端位姿
- [ ] 手部检测 / 跟踪 / 姿态（自研，非 EPIC 预标注）
- [ ] VLM 画质评分
- [ ] 场景自动分割
- [ ] 标注层（CEU）
- [ ] LeRobot / RLDS 训练格式导出
- [ ] A2D 真机数据源接入
