# 处理复盘：制造-鼠标套袋2（42.58s 视频耗时约 1 小时）

> 2026-08-07。单段 42.58s / 1278 帧的墨现 guida 数据，正常计算 ~35 分钟
> （手部推理 12min + 场景 VLM 5min + 转码/深度拷贝/脱敏 ~15min），
> 实际耗时接近 1 小时，其中 **~20 分钟是错误重跑与排查**。

## 时间账（按实际发生顺序）

| # | 环节 | 耗时 | 是否必要 |
|---|------|------|----------|
| 1 | main.py：QC + 手部推理（WiLoR 12min）+ 场景 VLM | ~20min | ✅ 必要（首次） |
| 2 | batch_prepare：输出目录含中文 → cv2 读不了深度 → 跑一段后失败 | ~8min | ❌ 浪费 |
| 3 | batch_prepare：未传 `-d`，默认读旧墨现数据（983 帧）→ 跑完整轮才发现 | ~12min | ❌ 浪费 |
| 4 | 主流程 VLM 瞬时失败降级（vlm_results=0），未被察觉 → 手动重跑场景 | ~5min | ❌ 浪费 |
| 5 | main.py 补跑：手部又推理了一遍（复用逻辑当时未加） | ~12min | ❌ 浪费 |
| 6 | batch_prepare 脱敏：GBK 崩溃（修 1 处）→ bbox 越界崩溃（修 1 处）→ 成功 | ~5min | 一半必要 |
| 7 | 目录归位（prepared → prepared_segments、scene 归位） | ~1min | 一次性整理 |

## 问题清单与根因

### A. 平台/环境坑（Windows）

| 问题 | 根因 | 现状 | 修复 |
|------|------|------|------|
| 输出目录含中文 → 深度 PNG 全读不了 | `cv2.imread()` 不支持非 ASCII 路径（静默返回 None） | 已规避（用英文目录），代码未防 | **P0** 见下方方案 2 |
| ffmpeg stderr GBK 解码崩溃 | `Popen(text=True)` 默认按 GBK 解码，ffmpeg 输出含 UTF-8 字节 | ✅ 已修（video_transcoder.py:337 加 `encoding="utf-8", errors="replace"`） | 排查其余 8+ 处同款调用（见 P1-4） |
| 后台任务 cwd 漂移 → `./.venv` 误用根级旧环境（无 cv2） | `E:\ZSPD\.venv` 是早期旧 venv，与项目 `.venv` 同名易混 | 未处理 | **P2-8** 删除根级旧 venv |

### B. 代码缺陷

| 问题 | 根因 | 现状 | 修复 |
|------|------|------|------|
| `-d` 默认硬编码旧数据集 | batch_prepare.py:77 `DATASET = "E:/datasets/egos/墨现"`，未传 `-d` 时静默读旧数据 | 未处理 | **P0-1** 改必填/None + 报错 |
| 手部产物每次重跑 | main.py 无复用机制，WiLoR 推理 12min 重复花 | ✅ 已修（校验 report 帧数一致则复用） | — |
| 主流程 VLM 瞬时失败静默降级 | `VLMUnavailableError` 捕获后仅 print，被日志截断看不到；无重试 | 部分（降级日志存在，无重试） | **P1-5** 加 2 次退避重试；降级写入 run_summary |
| 手部复用代码 `UnboundLocalError: _json` | `json` 只是函数内局部 import，我加的复用代码用到却未全局导入 | ✅ 已修（main.py 顶部 `import json`） | — |
| easyocr 全图 OCR bbox 越界（x2=1.015） | 兜底路径未 clamp，被 `_require_normalized_bbox` 拒绝抛错 | ✅ 已修（clamp + 归一化） | **P1-7** 补单元测试防回归 |
| 中文路径无预检 | 深度校验（validator.py:303、stage5_depth.py、depth_writer.py:324/429）直接用 `cv2.imread` | 未处理 | **P0-2** fail-fast |

### C. 流程/复用缺失

| 问题 | 根因 | 现状 |
|------|------|------|
| 跑完整轮才发现数据不对（帧数/时长） | 无「开始前一致性预检」，candidates 的帧数与实际源对不上要到最后 validation 才暴露 | 未处理（**P0-3**） |
| 场景产物不可复用 | 场景分割 + VLM 每次重跑（VLM 花钱花时间） | 未处理（**P1-6**） |
| 深度 PNG 全量重拷 | 无增量拷贝（文件存在 + 同大小跳过） | 未处理（**P3-10**） |
| 脱敏后视频变大 22M→65M | 脱敏强制重转码（二次编码抹细节风险），mp4v 编码效率低 | 已知，接受 |

## 改进方案（按优先级）

### P0 — 下次处理新数据前必须做（预估省 20min/次）
1. **batch_prepare `-d` 改必填**：默认值删除，未传直接报错列出可用数据集，杜绝「读错数据跑完整轮」
2. **中文路径 fail-fast**：batch_prepare/main.py 启动时检查 dataset/candidates/output 路径是否含非 ASCII，命中立即报错并提示英文目录（当前要跑一段才失败）
3. **数据一致性预检**：batch_prepare 开始前打印源 index 帧数/时长 vs candidates `advertised_frame_count`，不一致立即中止

### P1 — 防重跑（预估省 10min/次）
4. **subprocess GBK 全排查**：audio_decoder.py:206、epic_reader.py:236/312、dunjia_reader.py:346、depth_writer.py:138、preview.py:84、segment_writer.py:58、video_transcoder.py:41（ffprobe）等所有 `text=True` 调用统一加 `encoding="utf-8", errors="replace"`
5. **VLM 瞬时失败重试**：2 次退避重试；仍失败时把 `vlm_unavailable_reason` 写进 run_summary.json，控制台输出醒目 WARN
6. **场景产物复用**：scene/ 存在且 `frame_count` 与当前一致则跳过场景分割 + VLM（与手部复用同模式）
7. **bbox clamp 回归测试**：text_easyocr 补单测（构造越界 bbox 场景）

### P2 — 环境清理 ✅ 已完成
8. ✅ 已删除根级 `E:\ZSPD\.venv`（旧空壳环境，防止 cwd 漂移误用）
9. ✅ 「新数据处理 Checklist」已写入 `ZPDS/README.md`，PROJECT_MAP 命令速查加了指针

### P3 — 性能 ✅ 已完成
10. ✅ 深度 PNG 增量复用：目标已存在且与源同大小 → 直接拷贝跳过解码+重编码
    （`segment/depth_writer.py` read_file_rows，实测同大小≈同内容，PNG 无损编码确定性）
11. ✅ 脱敏重编码改 libx264：mp4v 中间产物 → ffmpeg `-c:v libx264 -crf 23` 重编码，
    实测 67.6 MB → 23.1 MB（-66%）；失败自动回退 mp4v（`batch_prepare._recode_h264`）

## 正确处理流程（下次直接照做）

```bash
# 1. 解压到 E:/datasets/egos/<英文名>/
# 2. 主流程（--with-hands --with-scene）
export PYTHONPATH="e:/ZSPD/ZPDS"
python -m zpds_prepare.main "E:/datasets/egos/制造-鼠标套袋2" --profile guida \
  --with-hands --with-scene --output output/<英文名>/
# 3. 批量 Prepared Segment + 脱敏（-d 必填，--output 传 .../prepared_segments）
python batch_prepare.py -d "E:/datasets/egos/制造-鼠标套袋2" --profile guida \
  --output output/<英文名>/prepared_segments --with-privacy
```
